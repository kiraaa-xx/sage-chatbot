{
  "name": "sage-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "cp backend/.env.example backend/.env && echo 'Now add your GROQ_API_KEY to backend/.env'"
  }
}
